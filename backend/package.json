{
  "name": "mlb-montage-maker-backend",
  "version": "0.1.0",
  "description": "Backend for MLB Montage Maker",
  "license": "MIT",
  "author": "mehraDotDev",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --watch ./server.js",
    "deploy": "gcloud app deploy app.yaml --stop-previous-version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@google-cloud/vertexai": "^1.9.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^1.4.5-lts.1"
  }
}
